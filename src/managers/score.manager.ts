export class ScoreManager {
    private _scene: Phaser.Scene;
    private _score: number;

    get scene() { return this._scene; }
    get score() { return this._score; }

    constructor(scene: Phaser.Scene) {
        this._scene = scene;
    }

    /**
     * Purpoose of the score manager
     * 
     * When the user
     * 
     * 
     */
}